interface:
  display_name: "Reply to Conversations"
  short_description: "Draft and send exact-revision replies"
  default_prompt: "Use $reply-to-conversations to draft and send a concise reply to the conversation I select."
dependencies:
  tools:
    - type: "mcp"
      value: "operational-inbox"
      description: "Operational Inbox reply and delivery tools"
      transport: "streamable_http"
      url: "https://operationalinbox.com/mcp"
